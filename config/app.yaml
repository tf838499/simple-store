application:
  timeout:
    read: 1000
    write: 1000
  port: 8080
  uploadPath: "./upload"
  mode: "debug"
  appId: "go"
  apiBaseRoute: "/api/v1"
  db:
    HOST: localhost
    PORT: 5432
    USER: postgres
    NAME: postgres
    PASSWD: postgres
